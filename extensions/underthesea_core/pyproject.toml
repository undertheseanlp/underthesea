[tool.poetry]
name = "underthesea_core"
version = "0.0.1"
description = "Underthesea Core (fast & fun)"
authors = ["Underthesea Team"]

[tool.poetry.dependencies]
python = "^3.5"

[tool.poetry.dev-dependencies]
maturin = "==0.9.4"