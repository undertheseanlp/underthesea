Skip to content

Search or jump to…

Pull requests
Issues
Marketplace
Explore

@phamsuong1997
0
0 135 QuangPHG/underthesea
forked from undertheseanlp/underthesea
 Code  Pull requests 0  Projects 0  Wiki  Security  Insights
underthesea/tox.ini
@QuangHPham QuangHPham undertheseanlpGH-243: update tox.ini
a54d1d6 12 hours ago
@rain1024 @QuangHPham @jerry-le @JackNhat @Vietdung113
40 lines (32 sloc)  1.13 KB

[tox]
envlist = py36

[testenv:flake8]
basepython=python
deps=flake8
commands=flake8 underthesea

[testenv]
setenv =
    PYTHONPATH = {toxinidir}:{toxinidir}/underthesea

commands =
    python -m unittest discover tests.word_tokenize
    python -m unittest discover tests.pos_tag
    python -m unittest discover tests.chunking
    python -m unittest discover tests.dictionary
    python -m unittest discover tests.feature_engineering
    python -m unittest discover tests.ner
    python -m unittest discover tests.test_corpus

    ; classification module
    pip install Cython
    ; pip install -U fasttext==0.8.3 --no-cache-dir --no-deps --force-reinstall
    ; python underthesea/util/data.py
    git clone https://github.com/facebookresearch/fastText.git
    pip install unidecode
    pip install fastText/
    pip install unidecode
    python -m unittest discover tests.classification

    ; sentiment module
    python -m unittest discover tests.sentiment

; If you want to make tox run the tests with the same versions, create a
; requirements.txt with the pinned versions and uncomment the following lines:
; deps =
;     -r{toxinidir}/requirements.txt

© 2019 GitHub, Inc.
Terms
Privacy
Security
Status
Help
Contact GitHub
Pricing
API
Training
Blog
About
