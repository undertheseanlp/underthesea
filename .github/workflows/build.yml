on: [ push ]

jobs:
    tests:
        run-on: ubuntu-latest
        steps:
            - name: Set up Python 3.6
                uses: actions/setup-python@v2
                with:
                    python-version: 3.6
            - name: Install tox
                run: pip install tox

