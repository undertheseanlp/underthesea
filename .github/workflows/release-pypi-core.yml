on:
  push:
    branches: [ GH-185 ]

jobs:
  build:
    name: Build and package Pypi
    runs-on: ubuntu-latest
    steps:
        - name: Checkout master
          uses: actions/checkout@v1
          with:
              python-version: 3.6